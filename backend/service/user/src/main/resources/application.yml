spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/users-service

    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
